from tabulate import tabulate

data = [
    ["Balance", "AQB, TOTAL_HV_AMT, BAL_UTIL", "Indicates deposit stability and liquidity"],
    ["Product", "TOTAL_PRODUCT_EXCL_SERVICES, PRODUCT_SCORE, CROSS_SELL_SUCCESS", "Measures product engagement and cross-sell depth"],
    ["Transaction", "TXN_FREQ, AVG_TXN_VALUE, TRANSACTION_SCORE", "Captures revenue-generating activity"],
    ["Digital", "DIGITAL_ENGAGE, TOTAL_DIG_CNT, DIGI_TO_PHYS", "Reflects digital adoption and cost efficiency"],
    ["Risk", "DORM_RISK, SEV_DORM_RISK, TXN_CONSISTENCY", "Penalizes for dormancy and inconsistency"],
]

headers = ["Group", "Features Used", "Business Rationale"]

print(tabulate(data, headers=headers, tablefmt="grid"))
