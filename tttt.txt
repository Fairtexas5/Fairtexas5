import pandas as pd
import plotly.express as px

# Assume df is your DataFrame

# Take a 0.1% sample (minute sample)
sample_df = df.sample(frac=0.001, random_state=42)

# Select 10 representative numeric columns
cols = [
    'END_OF_DAY_BAL', 'INT_AVLABL', 'AVG_BAL_MTD', 'AVG_BAL_QTD', 'AVG_BAL_YTD',
    'DR_AMT', 'CR_AMT', 'SAVINGS_BANK', 'FD', 'CA'
]

# Melt for boxplot
melt_df = sample_df[cols].melt(var_name='Variable', value_name='Value')

# Plotly boxplot
fig = px.box(melt_df, x="Variable", y="Value", points="outliers")
fig.show()

